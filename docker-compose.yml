services:
  itcomms:
    build: .
    container_name: itcomms-scripts
    env_file: .env
    ports:
      - "8002:8000"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    restart: unless-stopped
