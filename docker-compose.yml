services:
  itcomms:
    build: .
    container_name: itcomms-scripts
    env_file: .env
    ports:
      - "8002:8000"
    volumes:
      - .:/app
      - /app/__pycache__
    restart: unless-stopped
