services:
  itcomms:
    build: .
    container_name: itcomms-scripts
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    restart: unless-stopped
